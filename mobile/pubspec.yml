name: flutterflow_app
description: A Flutter application for mobile and web.

publish_to: 'none' # Prevent publishing to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/   # Carpeta donde se guardan recursos (opcional)
